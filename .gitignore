node_modules/
dist/

# Ledger — contains position data and execution history, never commit
data/*.db
data/*.db-shm
data/*.db-wal

# Structured logs
logs/*.log
logs/*.json

# Environment — NEVER commit real credentials
.env

# OS
.DS_Store
Thumbs.db
